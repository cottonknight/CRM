<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.ManagersDao"><!-- 写对应类名全路径 -->
	<select id="login" parameterType="Managers" resultType="Managers">
		select * from t_manager where m_name=#{m_name} and m_pwd=#{m_pwd}
	</select>
</mapper>